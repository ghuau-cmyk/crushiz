{
  "name": "sukiss",
  "version": "0.1.0",
  "private": true,
  "description": "App de compatibilité — câblage du cœur applicatif (Phase 2).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node scripts/migrate.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "express-rate-limit": "^7.4.0",
    "pg": "^8.13.1"
  }
}
